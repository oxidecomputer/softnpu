[workspace]
resolver = "2"

members = [
  "lib",
  "client",
]

[workspace.dependencies]
softnpu = { path = "lib" }

serde = { version = "1.0.147", features = ["derive"] }
serde_json = "1.0.87"
tokio = { version = "1.23.1", features = ["sync", "net"] }
